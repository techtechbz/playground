calculating_natural_number_quotient = 10 / 2
natural_number_quotient = 8.4 / 2.1
decimal_quotient = 9 / 2
calculating_decimal_quotient = 5.6 / 3.5
negative_number_quotient = 4 / -8
positive_number_quotient = -3 / -1
divide_by_zero_quotient = 0 / -8

# 以下の計算は「ZeroDivisionError」を出力する
# zero_division = -3 / 0

print(calculating_natural_number_quotient)
print(natural_number_quotient)
print(decimal_quotient)
print(calculating_decimal_quotient)
print(negative_number_quotient)
print(positive_number_quotient)
print(divide_by_zero_quotient)
# print(zero_division)
